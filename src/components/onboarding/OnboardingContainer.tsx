
import React from 'react';

const OnboardingContainer: React.FC = () => {
  // Remove automatic onboarding redirection logic
  // Onboarding is now only triggered after signup or via SmartBooking button
  return null;
};

export default OnboardingContainer;
